import React from 'react'

const About = ({dark}) => {
  return (
    <section className={`py-16 ${dark ? 'bg-gray-900' : 'bg-white'}`}>
      <div className="">
        
      </div>
    </section>
  )
}

export default About